export default {
    data () {
        return {
            schema: this.$inkline.form({
                input: {
                    value: 'Default value'
                }
            })
        };
    }
};
