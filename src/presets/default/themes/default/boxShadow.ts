import type { RawTheme } from '../../../../types';

export const boxShadow: RawTheme['boxShadow'] = {
    offsetX: 0,
    offsetY: '0.5rem',
    blurRadius: '1rem',
    spreadRadius: '-0.75rem',
    color: 'rgba(0, 0, 0, 0.15)'
};
