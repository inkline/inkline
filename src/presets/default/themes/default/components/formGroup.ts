import type { RawTheme } from '../../../../../types';

export const formGroup: RawTheme['components']['formGroup'] = {
    default: {
        adjacent: {
            margin: {
                top: 'var(--margin-top)',
                right: 0,
                bottom: 0,
                left: 0
            }
        }
    }
};
