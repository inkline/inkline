//
// Nav Variables
//
--nav-font-size := --font-size

--nav-item-disabled-color := --text-muted

--nav-item-padding-base := --spacers['1/2'] --spacers['1']
--nav-item-padding := -size-map(--nav-item-padding-base, --sizes, --size-multipliers)
