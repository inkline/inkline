//
// Breadcrumb sizes
//
for --size in --sizes
    .breadcrumb.-{--size}
        > .item
            padding-left: --breadcrumb-item-padding[--size]

            + .item
                &::before
                    padding-right: --breadcrumb-item-padding[--size]
