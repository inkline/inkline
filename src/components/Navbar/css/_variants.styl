//
// Navbar themes
// Add the required color variants to all navbar variations
//
for --variant-name, --variant in --variants['navbar']
    .navbar.-{--variant-name}
        color: -text-color-by-luminance(--variant['color'], --variant-color-light, --variant-color-dark)
        background-color: --variant['color']

