//
// Checkbox Sizes
//
for --size in --sizes

    .form.-{--size} .form-check
    .form-group.-{--size} .form-check
    .form-check.-{--size}
        &.-custom.-checkbox
            .form-input-label
                &::before
                    if --border-radius-enabled
                        border-radius: --form-check-checkbox-border-radius[--size]

                &::after
                    font-size: --form-check-checkbox-icon-size[--size]
