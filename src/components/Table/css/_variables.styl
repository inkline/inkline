//
// Tables
//

--table-cell-padding := .75rem
--table-cell-padding-sm := .3rem

--table-border-width := --border-width
--table-border-color := --border-color

//
// Variants
//
--table-variants := (brand monochrome-white neutral state)
--table-variant-color-light := --variant-color-light
--table-variant-color-dark := --variant-color-dark


--variants['table'] = -add-variant(--table-variants, { color: --color-groups })