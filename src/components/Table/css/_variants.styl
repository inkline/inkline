//
// Variants
//
for --variant-name, --variant in --variants['table']
    --table-variant-text-color = -variant-color-by-luminance(--variant['color'], --variant-color-light, --variant-color-dark)
    --table-variant-border-color = -variant-color-by-luminance(--variant['color'], lighten(--variant['color'], 5%), darken(--variant['color'], 5%))
    --table-variant-hover-color = -variant-color-by-luminance(--variant['color'], lighten(--variant['color'], 6%), darken(--variant['color'], 6%))
    --table-variant-stripe-color = -variant-color-by-luminance(--variant['color'], lighten(--variant['color'], 3%), darken(--variant['color'], 3%))
    .table.-{--variant-name}
        border-top-color: --table-variant-border-color
        color: --table-variant-text-color

        th, td
            background-color: --variant['color']

        // Bordered table
        //
        // Add borders to all sides of the table and table cells
        //
        &.-bordered
            &, th, td
                border-color: --table-variant-border-color


        // Striped Table
        //
        // Default zebra-stripe styles (alternating gray and transparent backgrounds)
        //
        &.-striped
            > tr:nth-of-type(odd)
            tbody > tr:nth-of-type(odd)
                th, td
                    background-color: --table-variant-stripe-color

        &.-hover
            tbody tr
                &:hover
                    th, td
                        background-color: --table-variant-hover-color