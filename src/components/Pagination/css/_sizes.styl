//
// Nav sizes
//
for --size in --sizes
    .pagination.-{--size}
        font-size: --pagination-font-size[--size]

        > .item
            border-radius: --pagination-item-border-radius[--size]
            padding: --pagination-item-padding[--size]
            min-width: --pagination-item-min-width[--size]
