@import '../../resources/css/config'

.radio
    position: relative
    cursor: pointer
    display: inline-block
    white-space: nowrap
    outline: none
    font-size: --font-size['md']

    + .radio
        margin-left: --spacer

    &.-disabled
        color: --text-muted
        cursor: default
