//
// Variables
//
--container-width := {
  xs: 100%
  sm: --breakpoints['sm'] - --gutter['sm']
  md: --breakpoints['md'] - --gutter['md']
  lg: --breakpoints['lg'] - --gutter['lg']
  xl: --breakpoints['xl'] - --gutter['xl']
}