//
// Breakpoints
//

--breakpoints-xs := 0
--breakpoints-sm := 30rem
--breakpoints-md := 48rem
--breakpoints-lg := 64rem
--breakpoints-xl := 75rem

--breakpoints := {
  xs: --breakpoints-xs
  sm: --breakpoints-sm
  md: --breakpoints-md
  lg: --breakpoints-lg
  xl: --breakpoints-xl
}

// Magic breakpoint
// Generates no media query, it is used for generating the blank infix in
// responsive utility classes
//
--breakpoints['_'] = 0
