{
  "name": "root",
  "private": true,
  "scripts": {
    "postinstall": "npm run bootstrap",
    "bootstrap": "lerna exec npm install && lerna link",
    "dev": "lerna run dev --parallel"
  },
  "devDependencies": {
    "lerna": "^3.20.2"
  }
}
