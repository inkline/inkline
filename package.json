{
  "name": "inkline",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "ready": "pnpm run build && pnpm run check && pnpm run test",
    "check": "vp check",
    "test": "vp run -r test",
    "build": "vp run -r build",
    "dev": "vp run website#dev",
    "dev:storybook": "pnpm --filter @inkline/storybook-app dev",
    "prepare": "vp config",
    "ci:version": "changeset version",
    "ci:prepublish": "pnpm run build",
    "ci:publish": "pnpm run ci:prepublish && changeset publish"
  },
  "devDependencies": {
    "@changesets/cli": "^2.29.4",
    "vite-plus": "catalog:"
  },
  "engines": {
    "node": ">=22.12.0"
  },
  "packageManager": "pnpm@10.33.4"
}
