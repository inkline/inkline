import { defineComponent } from '../../../../../utils';

export const formGroup = defineComponent({
    adjacent: {
        margin: {
            top: 'var(--margin-top)',
            right: 0,
            bottom: 0,
            left: 0
        }
    }
});
