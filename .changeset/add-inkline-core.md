---
"@inkline/core": minor
"@inkline/compiler": minor
---

Add @inkline/core authoring API package with typed primitives (defineComponent, createSignal, createMemo, etc.), JSX runtime, and control-flow helpers. Support external import forwarding in the compiler codegen so non-framework imports (e.g. styleframe) are emitted into each target's output.
