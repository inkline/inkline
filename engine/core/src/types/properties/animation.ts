import { TokenValue } from '../tokens';

export type AnimationProperty = {
    name: TokenValue;
    duration: TokenValue;
    delay: TokenValue;
    fillMode: TokenValue;
    iterationCount: TokenValue;
    direction: TokenValue;
    timingFunction: TokenValue;
    playState: TokenValue;
};
