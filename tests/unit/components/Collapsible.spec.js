import { mount } from '@vue/test-utils';
import Collapsible from 'inkline/components/Collapsible';

describe('Components', () => {
    describe('Collapsible', () => {
        // let wrapper;
        //
        // beforeEach(() => {
        //     wrapper = mount(Collapsible);
        // });

        it('should render correctly', () => {
            expect(true);
        });
    });
});
