export {
    defineConfig,
    defineGenerator,
    createContext,
    variable,
    selector,
    atRule,
    calc,
    css,
    color,
    ref,
    file,
    theme,
    defaultThemeName,
    darkThemeName,
    defaultDefinitionOptions
} from '@inkline/core';

export type * from '@inkline/core';
