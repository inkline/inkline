console.log("Runtime mount + DOM equivalence tests: deferred until SSR mount harnesses are wired.");
console.log(
  "Framework SSR deps are installed (react-dom, @vue/server-renderer, svelte, solid-js).",
);
console.log("The testing/mount.ts and testing/equivalence.ts harnesses will invoke them.");
