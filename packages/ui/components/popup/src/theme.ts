import { ComponentSize, ComponentStateColor, defaultComponentSizes, defaultComponentStateColors } from '@inkline/theme';

export const defaultPopupColor = 'info';
export const defaultPopupColors = defaultComponentStateColors;
export const defaultPopupSize = 'md';
export const defaultPopupSizes = defaultComponentSizes;

export type PopupColorVariant = ComponentStateColor;
export type PopupSizeVariant = ComponentSize;
