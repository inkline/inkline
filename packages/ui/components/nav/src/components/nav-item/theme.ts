import { defaultDefinitionOptions, DefinitionOptions, selector } from '@inkline/core';

export function useNavItemTheme(userOptions: DefinitionOptions) {
    const _options = { ...defaultDefinitionOptions, ...userOptions };
}
