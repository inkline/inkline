import type { Module } from '../../types';
import {
    core,
    animation,
    border,
    borderRadius,
    boxShadow,
    breakpoints,
    colors,
    components,
    grid,
    scaleRatios,
    sizeMultiplier,
    percentages,
    spacing,
    typography,
    transition,
    layers
} from '../../modules';

export const defaultModules: Module[] = [
    core,
    animation,
    border,
    borderRadius,
    breakpoints,
    boxShadow,
    components,
    colors,
    grid,
    layers,
    scaleRatios,
    percentages,
    sizeMultiplier,
    spacing,
    typography,
    transition
];
