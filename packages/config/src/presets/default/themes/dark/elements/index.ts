import { RawTheme } from '../../../../../types';
import { body } from './body';
import { code } from './code';
import { kbd } from './kbd';

export const elements: RawTheme['elements'] = {
    body,
    code,
    kbd
};
