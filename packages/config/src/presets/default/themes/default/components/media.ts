import type { RawTheme } from '../../../../../types';

export const media: RawTheme['components']['media'] = {
    default: {
        image: {
            margin: {
                top: 0,
                right: 'var(--margin-right)',
                bottom: 0,
                left: 0
            }
        }
    }
};
