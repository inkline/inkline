import { defineComponent } from '../../../../../utils';

export const media = defineComponent({
    image: {
        margin: {
            top: 0,
            right: 'var(--margin-right)',
            bottom: 0,
            left: 0
        }
    }
});
