import { defineElement } from '../../../../../utils';

export const address = defineElement({
    margin: {
        top: 0,
        bottom: 'var(--margin-bottom)',
        left: 0,
        right: 0
    }
});
