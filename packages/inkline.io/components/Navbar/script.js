import { INavbar, INavbarBrand, INavbarItems, INav, INavItem } from '@inkline/inkline';

export default {
    name: 'Navbar',
    components: {
        INavbar,
        INavbarBrand,
        INavbarItems,
        INav,
        INavItem
    }
};
