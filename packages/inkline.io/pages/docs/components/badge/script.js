import View from '@components/View';
import { IBadge } from '@inkline/inkline';

export default {
    name: 'BadgeView',
    layout: 'documentation',
    extends: View,
    components: {
        IBadge
    }
};
