import type { UserOptions } from '@inkline/types';

export const defaultToastContainerId = 'inkline-toast-container';

export const defaultToastAddonOptions: UserOptions['toast'] = {
    duration: 4000
};
