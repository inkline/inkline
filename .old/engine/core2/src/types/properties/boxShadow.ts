import { TokenValue } from '../tokens';

export type BoxShadowProperty = {
    offsetX: TokenValue;
    offsetY: TokenValue;
    blurRadius: TokenValue;
    spreadRadius: TokenValue;
    color: TokenValue;
};
