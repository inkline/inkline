{
  "name": "@inkline/tsc-guard",
  "version": "0.0.0",
  "private": true,
  "description": "Internal: repo-root `tsc` bin that fails loudly and points at the real type-check gate.",
  "bin": {
    "tsc": "./bin/tsc.mjs"
  },
  "files": [
    "bin"
  ],
  "type": "module",
  "scripts": {
    "test": "vp test",
    "check": "vp check"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "typescript": "catalog:",
    "vite-plus": "catalog:",
    "vitest": "catalog:"
  }
}
