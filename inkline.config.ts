import { defineConfig } from "@inkline/config";

export default defineConfig({
    theme: {
        default: {
            color: {
                primary: "#3f51b5",
            },
        },
    },
});
